# Laporan Praktikum Algoritma dan Struktur Data Jobsheet 2 

<h4>Nama : Izza Yaasmin Nabilla<h4>
<h4>NIM : 254107020226<h4>
<h4>Kelas : TI-1H<h4>

### Jawaban Latihan:

**1. Identifikasi Object di Sekitar**
* **Object:** Laptop
* **Atribut:** * `merk` (String)
    * `warna` (String)
    * `kapasitasRam` (int)
    * `isNyala` (boolean)
* **Method:**
    * `nyalakanLaptop()`
    * `matikanLaptop()`
    * `bukaAplikasi(String namaApp)`

**Kode Java:**
```java
public class Laptop {
    String merk, warna;
    int kapasitasRam;
    boolean isNyala;

    void nyalakanLaptop() {
        isNyala = true;
        System.out.println("Laptop menyala...");
    }
}


### Jawaban Latihan 2 & 3: Sistem Penjadwalan Kuliah
Pada sistem penjadwalan kuliah, terdapat beberapa objek utama:

1. **Object Dosen**
   * Atribut: `nidn`, `nama`, `spesialisasi`.
   * Method: `mengajar()`, `memberiNilai()`.
2. **Object Mahasiswa**
   * Atribut: `nim`, `nama`, `jurusan`.
   * Method: `inputKRS()`, `lihatJadwal()`.
3. **Object Mata Kuliah**
   * Atribut: `kodeMK`, `namaMK`, `sks`.
   * Method: `tampilkanDetail()`.

**Class Diagram:**

classDiagram
    class Dosen {
        +String nidn
        +String nama
        +mengajar()
        +memberiNilai()
    }
    class Mahasiswa {
        +String nim
        +String nama
        +inputKRS()
        +lihatJadwal()
    }
    class MataKuliah {
        +String kodeMK
        +String namaMK
        +int sks
        +tampilkanDetail()
    }


### Jawaban Latihan 4: Class Diagram Bangun Ruang
Berdasarkan gambar Prisma Segitiga dan Balok, berikut adalah struktur Class Diagramnya:


**Implementasi Kode:**
```java
public class BangunRuang {
    double panjang, lebar, tinggi, alas, tinggiPrisma;

    double hitungVolumeBalok() {
        return panjang * lebar * tinggi;
    }
    
    double hitungVolumePrismaSegitiga() {
        return (0.5 * alas * tinggi) * tinggiPrisma;
    }
}


### Jawaban Latihan 5: Analisis Sistem Pemesanan Kantin
Berikut adalah identifikasi objek untuk sistem kantin:

* **Pelanggan:** Atribut (`idPelanggan`, `nama`), Method (`pesanMenu()`).
* **Menu:** Atribut (`namaMenu`, `harga`).
* **Pesanan (Order):** Atribut (`idPesanan`, `totalHarga`), Method (`hitungTotal()`).
* **Kasir:** Atribut (`namaKasir`), Method (`prosesPembayaran()`).
* **Pembayaran:** Atribut (`metodePembayaran`, `status`).

**Implementasi Kode:**
```java
public class Pesanan {
    int idPesanan;
    double totalHarga;

    void hitungTotal(double harga, int jumlah) {
        totalHarga = harga * jumlah;
        System.out.println("Total yang harus dibayar: Rp " + totalHarga);
    }
}